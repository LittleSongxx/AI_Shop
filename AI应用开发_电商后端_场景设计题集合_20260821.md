# AI 应用 / 电商后端场景设计题集合（2026-08-21）

本题集是 [真实面试题与备考报告_20260821](./AI应用开发_Java后端_真实面试题与备考报告_20260821.md) 的配套入口。每题都要求先画权威边界，再讲主链路、失败分支、指标和验收。

## P0 场景

| 场景 | 主链路 | 必答边界 |
|---|---|---|
| AI 推荐导购 | 意图/约束抽取 -> BM25+向量召回 -> rerank -> Java 商品/报价/库存快照 -> 硬过滤 -> 解释 | 模型不能生成或执行不存在的商品；预算/排除词不能被 fallback 绕过 |
| 可信客服 RAG | 版本化知识 -> 混合检索 -> 最小证据 -> claim/citation -> 回答或拒答 | 过期、矛盾、注入和证据不足必须降级，不得凭常识补写 |
| 订单售后 Agent | 定位订单 -> 查询权威状态 -> 提案 -> 用户确认 -> Java 幂等写入 -> 终态核对 | 未确认不写；远程未知为 `INCONCLUSIVE`/人工复核；重复确认不重复副作用 |
| 秒杀/库存 | Redis 预占 -> Java 事务 -> MQ/补偿 -> 对账 | 超卖、重复消费、回滚和毒消息要有可观察终点 |
| 知识库发布 | 导入/切分/索引 -> 校验 -> 发布版本 -> 在线读取 | 新旧版本切换原子；删除、回滚和缓存失效可回放 |

## P1 场景

- Worker 重启、租约失效、MQ redelivery：恢复后只能有一个终态，必须保存 trace 和 state diff。
- 多租户 Agent/Memory：主体、租户、会话和工具 capability 分层授权；跨租户召回和导出必须 fail-closed。
- MCP 工具治理：工具清单、输入 schema、权限、风险等级、审批和审计要可查询；高风险动作必须显式确认。
- 高并发搜索：候选批量快照、超时预算、部分 Provider、缓存击穿和 ES 深分页分别治理，不能用平均延迟掩盖尾部。
- 支付/退款：回调验签、状态机、迟到回调、重复退款、补偿和人工复核要以 Java 账本为准。

## 设计验收清单

1. 权威数据和模型可生成内容是否分离？
2. 每个外部调用是否有 deadline、重试上限、熔断和错误分类？
3. 每个写操作是否有幂等键、事务边界、未知结果和人工兜底？
4. 是否能从请求 ID 追到 provider、工具、数据库状态和最终指标？
5. 是否有按切片的质量门禁、故障注入和可重放 bad case？

项目当前已对前四个 P0 场景有可复核实现和 v9 证据；知识库热更新回放、并发重投递、真实行为收益和容量曲线仍列为后续工作，不能提前宣称完成。
