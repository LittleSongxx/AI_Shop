{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 1,
  "links": [
    {
      "icon": "external link",
      "includeVars": false,
      "keepTime": true,
      "tags": [],
      "targetBlank": true,
      "title": "Tempo Trace Explorer",
      "type": "link",
      "url": "/explore?left=%7B%22datasource%22:%22aishop-tempo%22%7D"
    }
  ],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "aishop-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "editorMode": "code",
          "expr": "sum by (event, result) (rate(agent_episode_event_total[5m]))",
          "legendFormat": "{{event}} / {{result}}",
          "refId": "A"
        }
      ],
      "title": "Episode 事件写入速率",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "aishop-prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "editorMode": "code",
          "expr": "agent_episode_queue_depth",
          "legendFormat": "queue depth",
          "refId": "A"
        },
        {
          "editorMode": "code",
          "expr": "sum by (reason) (increase(agent_episode_dropped_total[5m]))",
          "legendFormat": "dropped / {{reason}}",
          "refId": "B"
        }
      ],
      "title": "Episode 队列与丢弃",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "aishop-prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "editorMode": "code",
          "expr": "histogram_quantile(0.95, sum by (le, stage) (rate(agent_stage_latency_seconds_bucket[5m])))",
          "legendFormat": "p95 {{stage}}",
          "refId": "A"
        }
      ],
      "title": "Agent 阶段耗时 p95",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "aishop-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "editorMode": "code",
          "expr": "sum by (tool, status) (rate(agent_tool_call_total[5m]))",
          "legendFormat": "{{tool}} / {{status}}",
          "refId": "A"
        }
      ],
      "title": "工具调用结果",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "aishop-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "id": 5,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "editorMode": "code",
          "expr": "sum by (result, rule) (rate(agent_response_verifier_total[5m]))",
          "legendFormat": "{{result}} / {{rule}}",
          "refId": "A"
        }
      ],
      "title": "确定性 Verifier 结果",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "aishop-prometheus" },
      "fieldConfig": { "defaults": { "min": 0, "max": 1 }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
      "id": 6,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "editorMode": "code",
          "expr": "sum by (dimension) (rate(agent_judge_score_sum[15m])) / sum by (dimension) (rate(agent_judge_score_count[15m]))",
          "legendFormat": "{{dimension}}",
          "refId": "A"
        }
      ],
      "title": "Shadow Judge 维度均分",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "aishop-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 24 },
      "id": 7,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "editorMode": "code",
          "expr": "sum by (result) (rate(agent_judge_evaluation_total[5m]))",
          "legendFormat": "judge / {{result}}",
          "refId": "A"
        },
        {
          "editorMode": "code",
          "expr": "sum by (reason) (rate(agent_judge_dropped_total[5m]))",
          "legendFormat": "dropped / {{reason}}",
          "refId": "B"
        }
      ],
      "title": "Shadow Judge 队列与结果",
      "type": "timeseries"
    }
  ],
  "refresh": "15s",
  "schemaVersion": 39,
  "tags": ["AI_Shop", "Agent", "Trace"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timezone": "browser",
  "title": "AI_Shop Agent Quality",
  "uid": "aishop-agent-quality",
  "version": 2
}
