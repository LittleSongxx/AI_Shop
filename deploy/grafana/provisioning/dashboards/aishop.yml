apiVersion: 1
providers:
  - name: AI_Shop
    folder: AI_Shop
    type: file
    # 允许在 UI 上改完再导出回文件，只读 provider 会让面板无法编辑
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
