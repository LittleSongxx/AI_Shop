# Human-adjudication return intake

`returns/` preserves the exact returned bytes. Both decision files pass structural checks. The project owner confirms human final approval with AI-assisted text output; downstream metrics therefore use the `HUMAN_APPROVED_AI_ASSISTED` tier and do not claim unaided human authorship. Raw identity mismatches remain visible here.
