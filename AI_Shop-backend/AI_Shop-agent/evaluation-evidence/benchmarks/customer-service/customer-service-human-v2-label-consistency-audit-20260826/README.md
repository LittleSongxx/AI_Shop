# Customer-service v2 label consistency — re-adjudication required

This immutable diagnostic package proves that the current 120-case gold uses conflicting taxonomy and slot policies. It does not alter labels or fabricate human review. Send only the open sheet plus taxonomy contract to each independent reviewer; reveal adjudication context only after both sheets are sealed.
