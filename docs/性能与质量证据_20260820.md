# 性能与质量证据（2026-08-20）

本报告只记录已经落盘、可通过 SHA 或结果文件复核的数据。`SYNTHETIC + local-live` 表示在本地运行了真实 Provider 或真实进程，但不是线上流量；小样本分位数只作样本描述，不是 SLO。没有可信人民币单价时，成本统一写作 `UNPRICED`。

## A. 本轮 Agent v2 真实本地样本

### A1. 取消订单全栈

结果：`AI_Shop-backend/AI_Shop-agent/benchmarks/results/task-success-live-v2/agent-v2-adaptive-c501735-20260820-cancel-golden-r6/summary.json`

| 指标 | 值 |
|---|---:|
| case | `live-cancel-confirmed-012` |
| 通过 | `1/1` |
| task success / execution completeness | `1.0 / 1.0` |
| tool selection / argument / terminal state | `1.0 / 1.0 / 1.0` |
| severe safety violations | `0` |
| orchestration | `workflow` |
| trace steps | `27` |
| latency | `1067 ms`（n=1） |
| input / output / total tokens | `0 / 0 / 0` |
| model calls | `0` |
| pending action | `CANCEL_ORDER -> CONFIRMED` |

事件完整链为 `ACTION_PROPOSED -> ACTION_CONFIRMED_BY_USER -> ACTION_TERMINAL`，订单状态由 `0` 变为 `4`。由于这条路径是确定性 Workflow，不能用它宣称模型吞吐或 token 效率。

脱敏 Trace：`docs/evidence/agent-traces/agent-v2-cancel-r6-20260820/`。`traces.json` SHA-256 为 `d3883ed12aef54cb0ffd6c4d8970cf44e091ddb126ff68f3f330c897b9bf20d3`；`report.md` SHA-256 为 `f57c8b2d2a356a2fd76b8c2af0671e39f6d83e1f726da51e3b8066456d953a99`。

### A2. 真实 Provider/RAG 政策问答

结果：`AI_Shop-backend/AI_Shop-agent/benchmarks/results/task-success-live-v2/agent-v2-adaptive-c501735-20260820-rag-policy-confirmation-r5/summary.json`

| 指标 | 值 |
|---|---:|
| case | `live-confirmation-policy-020` |
| 通过 | `1/1` |
| task success / provider completeness | `1.0 / 1.0` |
| orchestration | `single_agent` |
| model | `deepseek-v4-flash` |
| LLM calls | `1` |
| input / output / total tokens | `4847 / 224 / 5071` |
| latency | `3237 ms`（n=1） |
| BM25 / Vector / Embedding / Rerank | `1 / 1 / 1 / 1`，Embedding/Rerank 均成功 |
| fallback / write tools / severe violations | `0 / 0 / 0` |
| source refs | `2` |

该样本证明 Provider 调用、RAG runtime record、引用和最终回答可以形成完整链路；n=1 不足以推导总体质量、P95 或成本。

## B. 确定性质量回归

| 套件 | 实测 | 口径 |
|---|---:|---|
| Commerce runtime | `27/27`，工具/参数正确率 `1.0`，严重安全违规 `0` | E1，生产决策代码 + 确定性适配器 |
| AI safety | `18/18`，严重安全违规 `0` | E1，合成攻击集；不代表开放世界覆盖 |
| Search/RAG contract | `162/162` | E1，数据锁和 query-understanding contract；未采集 live Recall |

## C. Search v2 正式本地 Provider 结果（历史保留）

结果：`AI_Shop-backend/AI_Shop-agent/benchmarks/evidence/search-mature-v2/search-v2-64aa86e-final-20260814/summary.json`

- 数据：中文 `600` 商品 / `240` 查询（public `120`、fresh `80`、challenge `40`）；WANDS `42,994` 商品 / `202` 查询 / `32,919` 有效判断；另有 `47` 商品目录上的 `45` 条 ProductService 路径。
- 中文 fresh：Recall@3 `0.889583`，Recall@5 `0.996875`，NDCG@5 `0.975290`。
- WANDS full catalog：condensed NDCG@10 `0.795349`，condensed MRR@10 `0.979579`，bpref `0.343359`；未标注商品不当作负例。
- ProductService：Recall@10 `0.377778`，因此正式质量门禁 `passed=false` / `FAILED_RETAINED`。
- Provider 完整：Embedding、Rerank 均调用且无 fallback；这不改变质量门禁失败结论。

## D. RAG retrieval v4 正式本地 Provider 结果（历史保留）

结果：`AI_Shop-backend/AI_Shop-agent/benchmarks/evidence/rag-retrieval-live-v4/rag-v4-64aa86e-routefix-20260814/summary.json`

- 总计 `264` 条：`72` public/dev、`144` known regression、`48` one-shot fresh。
- fresh `n=36` 有效 ranking case：Recall@3/5 `0.805556`，MRR@10 `0.75`，NDCG@5 `0.764548`。
- fresh no-answer accuracy `1.0`，injection robustness `0.9167`，canonical citation correctness/coverage `0.7442 / 0.8056`。
- Provider 完整：fresh/dev 的 Embedding、Rerank、query expansion 均无失败或 fallback；正式门禁仍为 `FAILED_RETAINED`。
- fresh 端到端延迟：P50 `613.7034 ms`、P95 `2913.9297 ms`；Embedding P95 `987.5145 ms`、Rerank P95 `522.8315 ms`、Expansion P95 `2356.2503 ms`。fresh 各阶段样本少于 100，不能当总体 P95/SLO。

## E. RAG generation v4 正式本地 Provider 结果（历史保留）

结果：`AI_Shop-backend/AI_Shop-agent/benchmarks/evidence/rag-generation-live-v4/rag-generation-v4-64aa86e-postfix-20260814/summary.json`

- `60/60` 执行，runtime error `0`，严重安全违规 `0`，但 task success `39/60 = 0.65`。
- Token：`20,583` input + `2,542` output = `23,125`；repair 使用 `3,984 + 516` token。
- 自动质量：required claim completeness `0.8406`，claim citation support `0.8804`，canonical citation coverage `0.9783`，no-answer `1.0`，injection robustness `1.0`。
- 端到端延迟 P50/P95：`1845.2595 / 3449.8273 ms`；TTFT P50/P95：`1347.7654 / 2956.6272 ms`。
- 人工状态：`HUMAN_REVIEW_PENDING`；AI 初审不是独立人工标注。正式门禁结果必须保留，不能用后续 replay/rescore 覆盖。

## F. 失败证据与修复边界

本轮保留 `rag-policy-r1/r2/r3/r4` 四份失败结果。它们分别记录协议消息顺序错误、Worker 重启干扰、token budget 超限，以及知识库缺失“七天退货政策”导致的证据不足。修复后的 `confirmation-r5` 只说明另一条有权威证据的政策问题通过；不应把它改写成退款政策覆盖率或完整 Agent TSR。

## G. 尚未采集

- Agent v2 冻结全量 `44` 条的正式 live TSR：`NOT_COLLECTED`。
- `workflow/single_agent/multi_agent` 同 fixture、同模型的正式配对消融：`NOT_COLLECTED`。
- 授权真实用户、FCR、CTR/CVR、GMV uplift、线上容量和长期 SLO：`NOT_COLLECTED`。
- 可信人民币成本：`UNPRICED`，不能写成成本为零。
