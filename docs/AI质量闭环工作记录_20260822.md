# AI_Shop AI 质量闭环工作记录（2026-08-22）

> 只记录本轮最核心的排错、改造和可复核结果。Python 命令统一使用 Conda `shop`：`/home/song/miniconda3/envs/shop/bin/python`。

## 1. 初始问题

- Search、RAG、Agent 旧 benchmark、数据和分母并存，主线结果无法一眼复核。
- 只看总分会掩盖单 slice 失败；缺少硬约束、引用、终态和重复副作用证据。
- Provider usage、费用未知态、DB batch/N+1、故障恢复和本地延迟边界没有统一口径。
- 旧 final 说明与当前源码/运行状态容易漂移；历史失败结果需要保留但不能冒充 current。

## 2. 关键优化与排错

1. 收敛到 `evaluation/` v3 协议：统一 case、slice、trial、usage、state diff、fault 和 semantic shadow 字段；所有执行复用 preflight、脱敏、哈希和 fail-closed。
2. Search 固定七类切片，逐 slice 输出 Recall@3/5/10、MRR、NDCG、no-result、硬约束、Provider completeness、尾延迟和拒绝原因；加入预算/排除/无结果/精确型号/partial Provider metamorphic checks。
3. RAG 保留 lexical/fact/citation/faithfulness 硬门禁，增加 claim-level semantic shadow；记录 judge 版本、prompt/model fingerprint、span、证据 ID、超时和 abstain，失败记 `UNAVAILABLE`。
4. Agent 增加独立 `pass^5`/`pass^8` 重复运行、独立 trial/user/request/idempotency key、Episode/工具字段、Java authoritative before/after hash、结构化 diff 和重复副作用计数。
5. 增加故障注入 scope，覆盖检索、Embedding、Rerank、Java 快照、LLM、Redis checkpoint、Worker deadline、MCP、重试和重复确认；故障样本独立于正常质量分母。
6. 统一 usage/cost：Provider 缺 usage 标 `MISSING_USAGE`；无可信单价时 `costCny=null`，不把未知写成零。
7. 用隔离真实 MySQL 做 1/10/50/100 候选 batch 与 N+1 benchmark，补 round trip、连接使用、P50/P95、错误率和 rollback probe。
8. 建立 current/archive/lifecycle/SHA-256 链：v2 通过 final 和 v3-v8 失败 final 只读归档；`.runs` 只保留 v9 development/regression/final；v9 final freeze 后不重跑、不改分母。
9. 修复影响主线的业务问题：取消提案错误路由、RAG lexical 安全契约、幂等/库存/订单状态边界、Java-Python 内部断言和消息补偿路径。

## 3. 阶段结果

| 阶段 | Search | RAG | Agent | 结果 |
|---|---:|---:|---:|---|
| development v9 | 18/18 | 18/18 | 7/7 | 所有可见域/切片门禁通过 |
| regression v9 | 20/20 | 26/26 | 5/5 | 所有可见域/切片门禁通过 |
| final v9 | 50/50 | 50/50 | 25/25 | 一次执行，三域硬门禁通过 |

Final 关键数字：

- Search：Recall@3 `0.928030`，Recall@5/10 `0.962121`，MRR@10 `0.937500`，NDCG@10 `0.920521`；硬约束违规 `0`，Provider completeness `1.0`；P50/P95/P99 `269.6/797.3/940.6 ms`。
- RAG：retrieval、generation、required claim、citation、grounded faithfulness、no-answer、injection 门禁通过；invalid citation、严重安全违规、runtime error 均 `0`；query expansion provider failure `3`，均有安全 deterministic fallback。
- Agent：25 cases × 8 trials = `200/200`；`pass^8=1.0`、critical workflow pass power `1.0`、terminal correctness `1.0`、state diff match `1.0`；重复副作用、runtime error、严重安全违规均 `0`。
- Semantic shadow：final `50/50` 有完整追踪、disagreement `0`；`humanGroundTruth=false`，不能称人工准确率/一致性。
- Fault：`12/12` recovery contract 通过；其中 harness shadow 单独标识，不混入生产边界结论。
- DB：MySQL `8.4.11` 隔离库；100 候选 batch 为 `1` round trip，N+1 为 `100`；错误率 `0`，rollback probe 通过；不是线上 SLO。
- 工程校验：Python `1204 passed, 7 skipped`；Ruff、compileall、`git diff --check`、manifest tests `8 passed`、evaluation validate/verify 全通过；Java 26 模块 reactor `BUILD SUCCESS`；Web `33 passed`、Admin `10 passed`，两端 lint/build/bundle budget 全通过。

## 4. 重启后的排错结论

- Docker 中 AI_Shop MySQL、Redis、RabbitMQ、Nacos、ES、可观测栈和 Java 服务均已恢复；Agent 使用 `shop` 环境运行。
- Agent readiness 首次探测出现瞬时 `502`，随后稳定 `200`；`/health/ready` 的 MySQL、Redis、RabbitMQ、Worker、Java、MCP、ES mapping 全通过。该瞬时响应不改写 v9 evidence。
- manifest 失败原因是 claim document 仍指向已被新版替换的 20260819 报告；已改为 20260821 报告，并补齐其两个配套题集链接。

## 5. 证据边界

- v9 current：`release-20260822-ai-quality-v9` / `final-20260822-ai-quality-v9`；current `SHA256SUMS` digest 为 `94edeb894f3e2d36f597cb9cc9796d6e0e6f6b81da08f030b8723435e441c11a`。
- v2 通过 final、v3-v8 失败 final 均保留原始文件和哈希归档；失败结果不替换 current。
- final holdout 位于 ignored `.holdouts/`，不加入 Git；本轮不重跑、不修改 final evidence、Provider/source/suite fingerprint。
- 所有延迟是本地完整链路的描述性观测；没有 CTR/CVR/GMV、工业级个性化、生产容量、支付合规或人工语义真值证据。

## 6. 20260821 报告复核与联网调研后的后续优先级

报告新增的记忆增长/压缩/多租户、SkillHub 合规、知识污染、Redis Cluster 锁、慢 SQL、ES 分片、缓存失效、AI Coding 验收和指标证伪问题，与源码对照后分为：

| 优先级 | 下一步 | 验收证据 |
|---|---|---|
| P0 | Worker restart + MQ redelivery + lease 失效并发 replay；MCP 工具 capability/risk/authorization matrix | 每次只产生一个正确终态；越权/高风险调用全拒绝并可审计 |
| P0 | catalog/knowledge price-stock-version mutation replay | 检索到执行间变化时重新确认或 `INCONCLUSIVE`；删除/回滚不泄露旧事实 |
| P1 | Memory 规模、压缩损失、召回命中、租户隔离和 retention 指标 | 固定数据集、跨用户负向用例、增长曲线和可删除证明 |
| P1 | 统一 OpenTelemetry GenAI/agent/tool span、token、provider、错误字段 | 可按 request/episode/tool/provider 聚合并与 usage 对账 |
| P1 | Search recall/rerank/filter 的质量-延迟-成本 ablation；ES shard/慢 SQL/缓存失效曲线 | 每 slice 独立报告，不把本地 benchmark 写成生产 SLO |
| P1 | 小规模独立人工盲评校准 semantic shadow | 报告 judge-human agreement、abstain 和 disagreement；校准集不回灌 final |
| P2 | 真实曝光/点击/购买、偏差校正、线上 A/B；生成式检索/训练 ranker | 具备真实行为和授权数据后再评估，不提前宣称收益 |

本次外部核验的主要一手/原始来源：

- [MCP Authorization Specification](https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization)：HTTP MCP 的资源服务器、OAuth 发现、resource binding 和 token 安全边界。
- [MCP Specification](https://modelcontextprotocol.io/specification/2025-06-18)：client/server、capability negotiation、tools/resources 和 trust boundary。
- [LangGraph Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)：checkpointer 是 thread-scoped，store 才是跨 thread 长期记忆；持久化增长需要 retention/pruning。
- [OpenTelemetry GenAI semantic conventions](https://opentelemetry.io/docs/specs/semconv/gen-ai/)：模型、Provider、token、操作和错误字段应统一，当前项目还需补齐 agent/tool 语义映射。
- [OWASP LLM Top 10](https://genai.owasp.org/llm-top-10/) 与 [NIST AI RMF](https://www.nist.gov/itl/ai-risk-management-framework)：把注入、数据污染、权限、监控和风险治理落为可验证控制。
- [Elasticsearch rank evaluation](https://www.elastic.co/guide/en/elasticsearch/reference/current/search-rank-eval.html)：qrel/rank evaluation 支持检索分层评测，和当前 slice 设计一致。
- [Redis distributed locks](https://redis.io/docs/latest/develop/clients/patterns/distributed-locks/)：单副本 failover 可能破坏互斥；锁必须有唯一值、租约和明确故障假设。
- [MySQL 8.4 EXPLAIN](https://dev.mysql.com/doc/refman/8.4/en/explain.html)：用执行计划/实际执行统计验证慢 SQL，不能只凭应用耗时猜原因。
- [RAGChecker](https://arxiv.org/abs/2408.08067)、[tau-bench](https://arxiv.org/abs/2406.12045)、[RAGAS](https://arxiv.org/abs/2309.15217)：支持检索/生成分层、claim 诊断和最终状态驱动的 Agent 评测；论文结果不替代本项目真实 trace。
