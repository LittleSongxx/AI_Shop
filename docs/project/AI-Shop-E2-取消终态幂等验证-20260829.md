# E2 取消、终态与幂等验证（2026-08-29）

状态：`CONTROLLED_PREPROD_ONLY`。本批证明取消、HTTP 重试和迟到终态沿现有持久化状态机
收敛，不代表无人值守生产能力或线上 SLO。

## 本批收口

- `cancelMessage` 返回 `changed`、`messageStatus`、`taskStatus` 和 `terminalState` 等
  权威字段；`success=false` 不再被前端当成成功。
- 发送端稳定传递 `X-Request-ID` 与 `Idempotency-Key`；finalize/late-terminal 防护确保
  每个 run 至多一个终态和一次副作用。
- 取消与完成竞态、重复发送和 HTTP 重试继续由消息/任务 CAS 与既有幂等账本共同收敛。

## 可复核检查

| 范围 | 命令 | 结果 |
| --- | --- | --- |
| Agent 状态机/finalize | `uv run pytest -q tests/test_agent_finalize_precedence.py tests/test_agent_task_state_machine.py` | `43 passed` |
| Web cancel/status | `npm run test:unit -- --run tests/agent-confirm-card.test.ts tests/agent-websocket-manager.test.ts` | `2` files / `6 passed` |

## 限制

本批使用合成重试和竞态回归，未做全量 fault injection、生产部署或正式 SLO。Text2SQL
继续冻结，未修改其代码或历史证据包。
