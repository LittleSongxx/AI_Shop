# E1 流式与重连验证（2026-08-29）

状态：`CONTROLLED_PREPROD_ONLY`。本批把 Agent 流式消息的关联字段、OPEN barrier、乱序/重复
处理和终态回放边界固定下来；不代表线上丢包率、容量或生产 SLO。

## 本批收口

- `MessageSendDTO`、Python stream envelope 和前端 history reducer 兼容保留旧字段，并传递
  `schemaVersion`、`runId`、`requestId`、`episodeId`、`eventId`、`seq`、`terminalState` 和
  `replayCursor`。
- 前端等待 WebSocket `OPEN` 后再发送，按 `eventId/seq` 去重并按序拼接；收到 durable 终态
  后忽略迟到 Pub/Sub frame。
- 断线只回放现有持久化终态，不建设持久 chunk event-store；旧 API/事件名保持兼容。

## 可复核检查

| 范围 | 命令 | 结果 |
| --- | --- | --- |
| Agent stream | `uv run pytest -q tests/test_stream_service.py` | `3 passed` |
| Web reducer/OPEN barrier | `npm run test:unit -- --run tests/agent-message-reducer.test.ts tests/agent-websocket-manager.test.ts` | `2` files / `11 passed` |
| Java DTO | `mvn -q -pl AI_Shop-common -am -DskipTests compile` | passed |

## 限制

乱序、重复和终态竞态由合成回归覆盖；未启用外部服务 live 浏览器或高并发 soak。Text2SQL
继续按 [冻结说明](AI-Shop-Text2SQL冻结说明-20260829.md) 执行，本批未修改其代码或证据。
