# AI-Shop 运行态清理索引（2026-08-29）

状态：`QUARANTINED_REVERSIBLE`。服务已停止，Docker 中间件按原约定保留运行；本文件只登记外置路径和哈希，不把本地运行态重新纳入 Git。

## 外置 quarantine

- 根目录：`/home/song/AI_Shop-runtime-quarantine-20260829/`（目录权限 `0700`）。
- SHA 清单：`/home/song/AI_Shop-runtime-quarantine-20260829/SHA256SUMS`（文件权限 `0600`，49 条，清单 SHA-256：`644d7613494d52a4506e614b2bfaf7c15fd43e6623b758017432cf2e882396cf`）。
- 原文件合计：49 个、`11,634,909` bytes；已在 quarantine 内执行 `sha256sum -c SHA256SUMS`，49/49 通过。

| 外置内容 | 文件数 | bytes | 处理 |
| --- | ---: | ---: | --- |
| `run/logs/` | 19 | 1,994,237 | 停服务后可逆移出；含历史 Agent/Java/前端日志 |
| `run/observability/` | 3 | 1,769 | 停服务后可逆移出 |
| `run/text2sql-v0-runtime/` | 2 | 9,603,259 | Text2SQL runtime 日志，代码和证据包未改 |
| `run/runtime.env` | 1 | 2,614 | 含本机运行配置，未提交 |
| `run/java-build.stamp`、`run/start-stop.lock` | 2 | 0 | 运行态标记/锁，未提交 |
| `AI_Shop-backend/AI_Shop-agent/.privacy-exports/analytics/` | 22 | 33,030 | 已确认无活动导出后外置；精确目录仍在 `.gitignore` |

## 明确保留

`run/data/`、`run/backups/`、`run/recovery/`、`run/evaluation-observations/`、`run/review-workspaces/`、`run/review-imports/`、`run/evidence/` 和 `run/secrets/` 未移动；所有 tracked `evaluation-evidence/**`、历史失败包和根目录未跟踪 Text2SQL 证据也未修改。

若需要继续本地演示，重新执行 `./start.sh` 即可生成新的 `run/runtime.env`、PID 和日志；quarantine 原件只用于审计/恢复，不作为当前质量分母。
