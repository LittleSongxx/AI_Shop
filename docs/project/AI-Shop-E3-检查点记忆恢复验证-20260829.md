# E3 检查点与记忆恢复验证（2026-08-29）

状态：`CONTROLLED_PREPROD_ONLY`。本批收口 checkpoint/memory 的 revision/CAS 和生命周期
清理边界，不宣称跨区域容灾或生产容量。

## 本批收口

- checkpoint 与 session memory 写入带 revision/CAS；旧 revision 不能覆盖新 turn。
- 成功、handoff、cancel 清理 checkpoint；失败/unknown 只按 TTL 保留，取消无条件删除
  checkpoint。
- Redis 快速信号丢失时仍可从持久状态恢复；并发压缩和重启恢复不依赖单一进程内对象。

## 可复核检查

| 范围 | 命令 | 结果 |
| --- | --- | --- |
| checkpoint/memory lifecycle | `uv run pytest -q tests/test_checkpoint_lifecycle.py tests/test_session_memory_revision.py` | `13 passed` |

## 限制

本批未引入新的存储系统或跨租户迁移；未做长时间 soak、生产部署或正式 SLO。Text2SQL
继续按 [冻结说明](AI-Shop-Text2SQL冻结说明-20260829.md) 执行，本批未修改其代码或证据。
