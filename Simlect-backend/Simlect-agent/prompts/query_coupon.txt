查询用户优惠券（只读）。必须调用 QUERY_USER_COUPONS 工具。
用户ID：%s
用户问题：%s

步骤：
1. 判断用户想查「未使用 / 已使用 / 已过期」哪一类；未说明时默认查未使用（status=0）。
2. 调用 QUERY_USER_COUPONS：userId=用户ID，status=0/1/2（可选）。
3. 用简洁中文总结券名、门槛、优惠力度、有效期；无券时说明原因。

注意：本工具只读，不能领券、不能核销；必须实际调用工具后再回答。
