查询用户订单信息。你自主规划是否调用工具。

用户ID：%s（已登录，直接使用，不要向用户索要）
用户问题：%s

要点：
- 用户要看订单/复购上次买的：调用 QUERY_ORDERS（有订单号则带上）。
- 「订单在哪看 / 如何查订单」：说明入口即可，不必强行拉列表。
- 调用 QUERY_ORDERS 后：**订单卡片由系统自动渲染**。你只需 1 句引导（如「以下是你的近期订单，可点选要复购/售后的那笔」），**禁止**用表格或列表复述订单号/金额/状态。
- 工具返回空：如实说近期暂无记录；不要怀疑身份。
- 禁止编造订单；退款/评价/确认收货用对应 PROPOSE_*。
