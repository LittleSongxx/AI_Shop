# AI 应用 / Java 后端手撕与工程题集合（2026-08-21）

本题集是 [真实面试题与备考报告_20260821](./AI应用开发_Java后端_真实面试题与备考报告_20260821.md) 的配套入口。它只保留可在本项目中复现或解释的高频实现题；原帖来源、频率和岗位语境以主报告为准，不把题目清单当作录用概率。

## P0：必须能现场写并验证

| 主题 | 题目 | 现场验收 |
|---|---|---|
| 集合/缓存 | LRU、LFU、带过期的本地缓存 | O(1) 访问；容量、过期、更新和并发边界有测试 |
| 字符串/窗口 | 最长无重复子串、滑动窗口、括号匹配 | 空输入、重复字符、Unicode/边界下结果正确 |
| 链表/树 | 反转链表、链表判环、合并有序链表、层序遍历 | 不丢节点；时间 O(n)，空间和递归深度说清 |
| 数组/排序 | Top K、合并区间、最大子数组和、二分 | 重复值、负数、越界和稳定性有测试 |
| 并发 | 生产者消费者、线程安全计数、限流器 | 可见性、竞态、关闭、超时和背压有说明 |
| 交易工程 | 幂等键、库存扣减、Outbox/重试 | 重复请求不重复写；未知远端结果不伪造成功 |
| AI 工程 | 结构化 JSON 校验与修复、工具参数白名单 | malformed、缺字段、越权参数 fail-closed |

## P1：结合项目追问

- RAG：实现 BM25/向量候选融合、top-k 评测、引用校验和证据不足拒答；分别报告检索与生成指标。
- Agent：实现有限状态图、checkpoint 恢复、工具失败重试和确认后写入；用终态 state diff 验收，而不是只看回复文本。
- Java/Redis/MQ：解释 Redis 锁的租约边界、RabbitMQ confirm/ack/redelivery、事务消息补偿和重复消费幂等。
- 数据库：用 `EXPLAIN`/`EXPLAIN ANALYZE` 定位慢 SQL，比较批量查询与 N+1 的 round trip，并验证回滚。
- 安全：输入注入、工具结果污染、SSRF、租户/主体越权和审计字段都要有拒绝路径与测试。

## 统一作答模板

1. 先澄清输入规模、正确性边界、并发和失败语义。
2. 写最小可运行实现，再补时间/空间复杂度和关键不变量。
3. 用至少一个反例、一个超时/重试场景和一个幂等场景验收。
4. 说明生产化还需要的观测、限流、回滚、权限和数据证据。

## 与 AI_Shop 的对应证据

- 搜索：`evaluation` 的 slice/qrel、硬约束和 metamorphic checks。
- RAG：claim/citation/faithfulness、semantic shadow 和拒答门禁。
- Agent：Episode、工具参数、`pass^k`、before/after 状态 diff、故障矩阵。
- Java 底座：订单/库存/支付事务、消息补偿、批量 benchmark 和集成测试。

本题集不证明真实用户效果、生产吞吐或支付合规；这些仍需要独立环境和人工/业务证据。
