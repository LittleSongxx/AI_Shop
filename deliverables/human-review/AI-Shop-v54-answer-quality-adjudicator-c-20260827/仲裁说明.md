# v54 答案质量仲裁

本包只有 A/B 分歧的 `8` 条：`cs-gold-v1-026`、`036`、`048`、`cs-candidate-v2-090`、`092`、`096`、`110`、`116`。

仲裁人 C 只修改 `adjudication.open.jsonl` 每行的：

- `finalLabels`：填完四个最终标签。
- `adjudicator`：填稳定的仲裁人标识，不得是 reviewer-a/b。
- `reason`：填可复核理由。

不要修改其他字段。同时填写 `adjudicator-attestation.template.json`。AI 可辅助文字编辑和记录，但最终标签决策必须由人工确认。完成后原名返回这两个文件。
