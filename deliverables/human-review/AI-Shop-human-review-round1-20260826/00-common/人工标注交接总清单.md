# 人工标注与独立证据交接总清单（2026-08-26）

> 本页是当前唯一人工工作入口。自动代码、规则、LLM 自评、旧 gold、行为契约和开发者判断都不能替代这里要求的独立真人工作。现有 immutable evidence 不得原地修改；每条工作流必须生成新文件、新哈希和新 package。

## 当前状态

| 工作流 | 数量 | 当前状态 | 是否阻断发布级质量 |
|---|---:|---|---|
| A. 客服 v2.1 标签政策重仲裁 | 25 | 空白盲标模板已准备；真人未开始 | 是 |
| B. v43 冻结答案双人盲审 | 120 | A/B 两份空表已准备并校验；真人未开始 | 是 |
| C. v2 来源独立性复核 | 12 起步 | 空白独立复审表和声明模板已准备；真人未开始 | 是 |
| D. 全新 external unseen final | 至少 100 | 仅规范已准备；当前 125 条永久失去 unseen 资格 | 是 |

当前 120 条客服数据 SHA-256 为 `ab5129a73cf6f986173d92e3f5f04ab7e8689bae9ad4c7d7294fa13b587ee079`。它的 hash/label chain 可验证，但 label consistency 状态为 `BLOCKED_HUMAN_READJUDICATION`，reviewer independence 状态为未验证；只能作 development diagnostic。

四条工作流必须由不同输入和生命周期处理，不能互相复制标签、合并分母或用一条工作的结论替代另一条。

## A. 客服 v2.1 标签政策重仲裁（25 条）

### 交付文件

- 审计包：`/home/song/code/Java/AI_Shop/AI_Shop-backend/AI_Shop-agent/evaluation-evidence/benchmarks/customer-service/customer-service-human-v2-label-consistency-audit-20260826/`
- 盲标模板：`reaudit/label-policy-reaudit.open.jsonl`
- 模板 SHA-256：`f40ebda1c3b5c6625b91e6471256c743ce1a05c88a2906db52dfe2a14e5c8e54`
- 模板 manifest：`reaudit/label-policy-reaudit.open.jsonl.manifest.json`
- taxonomy：`source/customer-service-taxonomy-contract-v2.1.json`
- 仅在 A/B 均封存后交给第三人的上下文：`reaudit/adjudication-context.after-sealing.jsonl`

模板不包含 current gold 或模型预测，覆盖 5 项一致性发现的并集 25 条。两位独立 reviewer 各自取得一份只读复制件，必须使用不同稳定 reviewer ID，且在各自封存前不得看到另一人的标签、当前 gold、规则预测或仲裁上下文。

### 标注口径

- 推荐、换一批、再推荐和基于上一批修改条件都归 `PRODUCT_SEARCH`。
- `RECOMMENT` 仅指绑定订单的追评/追加评价写提案，不表示重新推荐。
- `amount` 保存原文可见完整 span；归一化数值是下游派生字段，不能覆盖 raw gold。
- `budget` 作为独立显式约束标注；不得因已有 `amount` 而省略。
- item 数量与“投诉三次/扣款两次”等 occurrence/frequency 分开；后者不写入商品 `quantity`。
- `productName` 与 `feature` 使用不重叠 span，不能把同一“降噪”同时重复写进两个实体。

上述政策是 v2.1 successor 的预注册口径；不得为了贴合当前实现逐 case 改规则。

### 完成门槛

1. 两份 25 条表均完整，无 gold/model 泄漏，reviewer ID 不同。
2. 分别封存填写后原始 bytes、表 hash、初始模板 hash、taxonomy hash、reviewer identity/independence attestation。
3. 先生成一致性报告，再只把全部分歧交给独立第三人；第三人必须给出原文依据和最终五字段标签。
4. 基于旧 120 条 immutable source 生成新 hash 的 120 条 v2.1 successor；未受影响的 95 条保持逐字节投影一致，25 条只采用 A/B 一致或第三人仲裁结果。
5. 在 successor 上重跑 rule 和 HTTP，重新计算指标、区间和 badcase；当前 v43 数值不能继承。

## B. v43 冻结答案双人盲审（120 条）

### 交付文件

工作区：

`/home/song/code/Java/AI_Shop/AI_Shop-backend/AI_Shop-agent/run/review-workspaces/customer-service-http-v43-human-v2-routing-execution-fix-20260826/`

- reviewer-a：`reviewer-a.open.jsonl`
  - SHA-256：`bf13a3aeb2cf2f2d5064388776af1a2318609585cd96bdde11e7f5815a8adc70`
- reviewer-b：`reviewer-b.open.jsonl`
  - SHA-256：`5782adb16467463445b2d815a6d09b983434faa0d4e078ff6854d2625a52712e6`

两表各 120 行、顺序不同、标签为空，不含 expected、规则/HTTP intent 预测、质量分数或另一位 reviewer 标签。两表共同绑定：

- source report：`evaluation-evidence/benchmarks/customer-service/customer-service-http-v43-human-v2-routing-execution-fix-20260826/report.json`
- report SHA-256：`c8efc7d69e4e85f1478c74c6365f749b36842168f8e08cb3f86a2244704498d3`

每条需独立填写：

- `answerCorrect`
- `citationSupport`：`SUPPORTED` / `UNSUPPORTED` / `UNDECIDABLE` / `NOT_APPLICABLE`
- `handoffAppropriate`
- `unsafeAnswer`

`citationStructureInvalid=0`、verifier pass、contract pass 或 safe degradation 都不是 `citationSupport=SUPPORTED`/`answerCorrect=true` 的依据；reviewer 必须阅读具体 answer 与 sourceRefs。

### 回传与校验

所有 Python 命令必须使用 `shop`：

```bash
cd /home/song/code/Java/AI_Shop/AI_Shop-backend/AI_Shop-agent

REPORT=evaluation-evidence/benchmarks/customer-service/customer-service-http-v43-human-v2-routing-execution-fix-20260826/report.json
WORK=run/review-workspaces/customer-service-http-v43-human-v2-routing-execution-fix-20260826

conda run --no-capture-output -n shop python -m evaluation.cli customer-service-http review-validate \
  --report "$REPORT" --review "$WORK/reviewer-a.open.jsonl" --complete
conda run --no-capture-output -n shop python -m evaluation.cli customer-service-http review-validate \
  --report "$REPORT" --review "$WORK/reviewer-b.open.jsonl" --complete

conda run --no-capture-output -n shop python -m evaluation.cli customer-service-http review-seal \
  --report "$REPORT" --review "$WORK/reviewer-a.open.jsonl" \
  --output "$WORK/reviewer-a.sealed.jsonl"
conda run --no-capture-output -n shop python -m evaluation.cli customer-service-http review-seal \
  --report "$REPORT" --review "$WORK/reviewer-b.open.jsonl" \
  --output "$WORK/reviewer-b.sealed.jsonl"

conda run --no-capture-output -n shop python -m evaluation.cli customer-service-http review-package \
  --report "$REPORT" \
  --review-a "$WORK/reviewer-a.sealed.jsonl" \
  --review-b "$WORK/reviewer-b.sealed.jsonl" \
  --output-dir evaluation-evidence/benchmarks/customer-service/customer-service-http-v43-answer-review-pending-adjudication-20260826 \
  --adjudication-output "$WORK/adjudication.answer-review-v43.open.jsonl"
```

如果分歧数为 0，不能伪造第三人仲裁；按 agreed/no-adjudication 生命周期封存。若有分歧，则独立 reviewer-c 只填写自动导出的分歧模板，之后执行 `review-merge` 创建新的 adjudicated package。任何命令都不得覆盖已存在目录。

## C. v2 来源独立性复核（12 条起步）

### 交付文件

来源包：

`/home/song/code/Java/AI_Shop/AI_Shop-backend/AI_Shop-agent/evaluation-evidence/benchmarks/customer-service/customer-service-human-v2-provenance-pending-20260826/`

- 盲复审表：`reaudit/independent-reaudit.open.jsonl`
- SHA-256：`d76e51dc4ab8bce6c451ca8a41e573b3bf492dcb1bbd9596f21fd71fd5ae8386`
- manifest：`reaudit/independent-reaudit.open.jsonl.manifest.json`
- 独立性声明：`reaudit/independence-attestation.template.json`
- 声明模板 SHA-256：`7ae8afffe9ea876a69dd4f8ec1109529b81ef31ad16eb9df2476580c650582ce`

独立保管人只把盲复审表与冻结 guideline 交给 reviewer。reviewer 不得看到 source human dataset、draft labels、模型输出、旧 A/B 表、agreement 或 adjudication。

预注册门槛：

- critical mismatch `0`
- intent agreement `>=0.80`
- risk agreement `>=0.90`
- handoff agreement `>=0.90`
- slot exact agreement `>=0.70`

任一门槛失败，必须扩大到 additions 全 60 条独立复审。通过只支持“历史标签可靠性得到额外复核”，不能把已对开发者可见的 120 条恢复成 unseen。原 provenance package 的四项 HIGH finding 也不能被删除或原地改写，只能由 successor package 记录新证据。

## D. 全新 external unseen final（至少 100 条）

当前 `/home/song/AI_Shop-external-unseen-final-20260826/` 的 125 条正文已经对开发者可见，状态永久为 `DISQUALIFIED_DEVELOPER_VISIBLE`。即使现在补签声明、双评和仲裁，也不能恢复 unseen；它只适合协议联调。

正式替代批次必须遵循 [`external-unseen-final 独立生成与保管规范`](external-unseen-final-independent-generation-and-custody-20260826.md)：

1. 在 clean commit 或已验证 source freeze 后，由未参与当前数据与实现优化的 custodian 独立生成。
2. 开发者在运行前只能得到 case count、域分层、schema 和数据 hash，不能看到正文、qrel、事实标签或 expected。
3. 先做源码暴露、历史 final、development/regression、客服 v1/v2 和 badcase overlap 审计；任一不可解释重叠则拒绝消费。
4. 一次性运行并登记 consumption；模型输出冻结后再交双人评审与分歧仲裁。
5. Search/RAG/Agent/客服分别报告分子、分母、区间和 badcase，不能合并成没有业务含义的单一总分。

## 人工工作的共同禁止事项

- 不查看候选 `expected`、当前 gold、规则预测、模型自评、历史 v9/v27 标签或另一 reviewer 的中间结果。
- 不删 case、不改 ID、不改用户原话；不知道时使用协议允许的 `UNDECIDABLE`，不猜测。
- 不把 reviewer agreement、静态契约通过、HTTP 生产执行或 verifier pass 写成答案语义质量。
- 不把 label-policy 复审、provenance 复审、answer review 和 external final 混成同一分母。
- 不在已有 evidence package 内编辑任何文件；新结果必须写入新目录并重新生成 `SHA256SUMS`。

## 当前质量边界

在四条外部工作完成前，权威状态保持：

- 当前 v2：`DEVELOPMENT_DIAGNOSTIC_ONLY`
- v43：生产执行 `120/120`、行为契约 `22/22`，但答案人工评审 `0/120`
- `releaseGateEligible=false`
- `finalUnseenEligible=false`
- 不报告 v43 answer correctness、citation support、unsafe rate，不报告生产 SLO、CSAT/FCR 或业务收益

历史 v27 的 `59/60` 答案和 `35/36` 引用仅绑定历史 60 条冻结输出；不能迁移到 v43，也不能替代新 120 条双审。
