# v56 答案质量仲裁

本包只有 A/B 分歧的 `2` 条：`cs-gold-v1-026`、`cs-candidate-v2-096`。

仲裁人 C 只修改 `adjudication.open.jsonl` 每行的：

- `finalLabels`：填完四个最终标签；
- `adjudicator`：填写稳定的仲裁人标识，不得是 `reviewer-a` 或 `reviewer-b`；
- `reason`：填写可复核理由。

不要修改其他字段。同时填写 `adjudicator-attestation.template.json`。AI 可辅助录入和文字编辑，但最终标签决策须由人工确认。完成后原名返回这两个文件。
