# AI_Shop Agent 优化实施结果与证据边界

> 核验日期：2026-08-17（Asia/Shanghai）
>
> 状态：核心代码与评测契约已落地；真实模型 Agent 全栈结果、三模式 live 消融和正式 Trace 尚未采集。

## 实施结果

本轮没有继续堆独立功能，而是把项目收敛为一条可追问、可验证的售后 Agent 闭环：

```text
自然语言订单诉求
  -> 歧义解析
  -> RAG 售后政策 + MCP/Java 权威事实
  -> 退款提案
  -> 用户确认
  -> Java 鉴权、状态校验与幂等执行
  -> 未知结果进入人工复核
  -> Episode、冻结任务集、消融和脱敏 Trace
```

### 1. 真实全栈任务评测契约

- 冻结任务集由 37 个唯一 case 组成，覆盖权威查询、写提案、RAG、跨域、安全、澄清、购物和未知结果。
- 数据集使用真实 fixture 占位符并由 SHA-256 lock 固定；私有 token、用户和订单绑定文件不入库。
- Runner 只调用 `POST /api/agent/sendMessage`，执行路径为 API -> RabbitMQ -> Worker -> LangGraph -> MCP -> Java -> DB。
- 判分只读取持久 Episode、消息和 pending-action 状态；依赖、Provider、fixture 或调用证据不完整时 fail-closed。
- RAG 样例逐条核验 embedding/rerank 的真实请求、成功、失败、缓存命中和 fallback。

当前 `task_success_v1.lock.json` 明确记录 `resultStatus=NOT_COLLECTED`。TSR 80% 是门禁，不是已经取得的成绩。

### 2. 自适应编排与真实消融能力

- `workflow` 处理简单权威查询或参数完整的提案。
- `single_agent` 处理开放问题、单域 RAG 和澄清。
- `multi_agent` 只处理订单事实与政策等跨域复合任务。
- 固定模式支持在同一冻结数据、同一 fixture 快照和同一模型集合上采集三次 live 报告。
- 比较器强制检查数据 SHA、case、fixture、模型、Provider 指纹和 100% 执行/Provider 完整性，再输出配对成功率、延迟、token、成本及 bootstrap 95% CI。

旧确定性消融中，单 Agent 与多 Agent 均为 27/27，Workflow 适用集为 21/21；多 Agent 没有成功率增益，且步骤和延迟更高。该结果不能改称真实模型消融。

### 3. 写操作安全闭环

- “只解释退款条件”不会因识别到订单而生成 `PROPOSE_REFUND`。
- 模型只创建退款提案；用户确认后 Java 重新做身份、归属、状态和幂等校验。
- 远端结果未知时进入 `INCONCLUSIVE`，自动核对到边界后进入 `MANUAL_REVIEW`，不自动重放可能已成功的写操作。
- 未接入生产路径的通用 `retry_decorator.py` 已删除。MCP 只在 session lost 时单次重建；Worker 使用持久任务状态退避。

### 4. 运行时护栏

- 预算守卫按运行隔离 token、人民币成本、节点步数和 monotonic deadline。
- 达到 80% 记录预警，超限进入明确受控终态。
- 熔断器实现 CLOSED/OPEN/HALF_OPEN、并发 half-open probe 和恢复窗口，并接入真实调用路径。
- 预算和熔断解决资源与下游保护，不能替代 Java 业务幂等。

### 5. 面试 Trace 导出

- 导出器要求一条真实 `CONFIRMED` 退款和一条真实 `INCONCLUSIVE/MANUAL_REVIEW` 未知结果。
- 证据来自持久 Episode 与 MySQL pending-action 行。
- 输出 `traces.json`、`report.md`、`manifest.json` 和 `SHA256SUMS`。
- PII、凭据、action token、用户/订单标识和长业务 ID 会被脱敏。

服务栈尚未满足正式采集条件，因此当前只有导出能力和测试，不存在伪造的正式 Trace 包。

## 本轮已验证

| 验证 | 结果 |
|---|---:|
| Java 售后核心定向单测 | 37/37 |
| Order 模块本轮单测 | 75/75 |
| `TransactionPersistenceIT` | 3/3，Testcontainers MySQL 8.4.11 |
| Agent Python 全量回归 | 1114 passed / 7 skipped / 0 failed |
| RAG/评测扩大回归 | 94/94 |
| embedding、live task evaluator、消融定向回归 | 23/23 |
| Ruff 定向静态检查 | 通过 |

7 条 skip 均为显式要求真实 MySQL 8 的迁移集成测试；全量回归另有 1 条 LangGraph 依赖弃用预警。Ruff 全目录检查、29 项 manifest 普通/本地结果检查和 `git diff --check` 均通过。

后续全量回归结果应以本文件最后一次更新和 [evidence-manifest.json](docs/evidence-manifest.json) 为准；不要把不同测试命令的样例数相加成“独立覆盖总数”。

## 尚未采集

- 37 条任务的真实模型全栈 TSR、工具/参数准确率、P50/P95、TTFT、token 和人民币成本。
- workflow、single_agent、multi_agent 三次等价快照的 live 配对消融。
- 一条真实成功退款与一条真实未知结果的脱敏 Trace 包。
- 真实用户任务成功率、FCR、CTR/CVR、GMV uplift 或生产 SLO。
- 新的未暴露 RAG holdout；现有 v4 正式 fresh `Recall@5=0.8056`、生成 `39/60`，门禁未通过。

## 面试正确口径

> 项目已经具备真实全栈评测和证据导出的能力，但我没有把“评测框架可运行”写成“正式成绩已取得”。当前能证明的是受控写操作、未知结果人工复核、自适应编排、逐运行预算、Provider Trace、Java 幂等与 Testcontainers 持久化语义。Agent live TSR 和三模式消融要在真实 Provider、完整服务和可恢复业务 fixture 就绪后采集。

详细项目卡见 [个人技术贡献边界_项目卡片格式.md](docs/个人技术贡献边界_项目卡片格式.md)，评测操作见 [benchmarks/README.md](AI_Shop-backend/AI_Shop-agent/benchmarks/README.md)。
